{
    "api_path": {
        "description": "API接口路径",
        "type": "string",
        "default": "/api/render/image"
    },
    "auth_token": {
        "description": "认证令牌 (可选，用于API安全认证)",
        "type": "string",
        "default": ""
    },
    "server_host": {
        "description": "HTTP服务监听地址",
        "type": "string",
        "default": "0.0.0.0"
    },
    "server_port": {
        "description": "HTTP服务端口",
        "type": "int",
        "default": 11451
    },
    "render_quality": {
        "description": "默认渲染质量（low/medium/high/ultra，映射 72/144/200/300 PPI）",
        "type": "string",
        "default": "high"
    },
    "font_cache_dir": {
        "description": "在线字体缓存目录（相对路径基于插件目录解析，绝对路径按原样使用）",
        "type": "string",
        "default": "font_cache"
    },
    "font_download_timeout": {
        "description": "单个在线字体下载超时（秒）",
        "type": "int",
        "default": 30
    },
    "templates": {
        "description": "自定义模板（JSON 格式：{\"别名\": {\"name\": \"...\", \"description\": \"...\", \"typ_content\": \"...\", \"render_quality\": \"high\"}}）",
        "type": "string",
        "default": "{}",
        "editor_mode": true,
        "editor_language": "json"
    }
}